---
name: design-index
description: Index of .agents/design/ — the design system this panel is built against, and how this application applies it.
---

# Design Index

**Scope:** `.agents/design/`
**Parent:** [root-index](root-index.md)

The design system is a **standing constraint on what gets written**, not reference material:
a page that ignores it is wrong in the same way a branch named against the convention is.

Any file added to or removed from `.agents/design/` is reflected here in the same commit.

## Files

| File | Purpose |
|---|---|
| [`silver-glass.md`](../design/silver-glass.md) | The visual language: tokens, the glass recipe, the overlay rule, the component vocabulary, and the accessibility checklist. Portable, and the source of truth. |
| [`panel-application.md`](../design/panel-application.md) | How this single-page application applies it: the class vocabulary in use, what replaces the runtime includes and per-page CSS linking, and the rules that bind when adding a page. |
