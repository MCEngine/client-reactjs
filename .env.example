# Vite inlines every VITE_-prefixed value into the bundle. Nothing here is
# secret, and nothing secret may be added -- see .agents/rules/repository.md.

# Where the API lives. Empty means same-origin, which is what the dev proxy and
# a production deployment behind one hostname both give you.
VITE_API_BASE_URL=
